#include<bits/stdc++.h>
using namespace std;

int main(){

    //para or line with spaces or tab
    // string s;
    // getline(cin, s);
    // cout << s;

    //size of a string
    string s;
    getline(cin, s);
    cout<<s<<endl;
    cout<< s.size();
}